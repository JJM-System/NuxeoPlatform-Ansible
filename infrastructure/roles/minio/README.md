# Role: minio

MinIO S3-compatible object storage for Nuxeo binary store

## Variables

See `defaults/main.yml` for all tunable variables with their default values.

## Dependencies

None.

## Example Playbook

```yaml
- hosts: <target_group>
  roles:
    - role: minio
```
